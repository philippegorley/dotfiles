[user]
    email = gorley.philippe@gmail.com
    name = philippegorley
[push]
    default = simple
[alias]
    #all = "!f() { ls | xargs -I{} git -C {} $1; }; f"
    all = "!f() { ls -R -d */.git | sed 's,/.git,,' | xargs -I{} git -C {} $1; }; f"
    unstage = reset HEAD
    undo = reset --soft HEAD^
    back = reset --hard HEAD~
    fire = "!git commit -a -m 'FIRE' && git push origin fire-branch"
[color]
    ui = true
[pull]
    rebase = true

